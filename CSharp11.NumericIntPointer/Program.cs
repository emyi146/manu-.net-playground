
// Represents an integer where the bit-width is the same as a pointer
IntPtr intPtr = IntPtr.Zero;    // signed
UIntPtr uIntPtr = UIntPtr.Zero; // unsigned


// In C# 11 it has an alias nint and nuint
nint intPtr2 = nint.Zero;
nuint uIntPtr2 = nuint.Zero;